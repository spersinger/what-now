# Installing dependencies
All the deps are in the requirements.txt python file. It should be as simple as running
```bash
pip install -r requirements.txt
```
within a venv to install. The venv is not necessary but might be useful for controlling which versionsof the dependencies are installed.

## Dependencies installed so far
- PyAudio
- KivyBase
